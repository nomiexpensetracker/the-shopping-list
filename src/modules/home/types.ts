/** Form fields for the Quick Shop session creation form. */
export interface QuickShopForm {
  name: string;
  title: string;
}

/** Form fields for the New List creation form. */
export interface NewListForm {
  name: string;
}
